var carteroSass = require( "cartero-sass" );
var fs = require( "fs" );

var myStream = carteroSass( "./greenHeader.scss" );

var outputStream = fs.createReadStream( "./greenHeader.scss" );

outputStream.pipe( myStream ).pipe( process.stdout );
